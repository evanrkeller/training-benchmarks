.page-header
  = link_to practices_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    = t('back')
  = link_to @practice, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-info-sign
    = t('show')
  %h1= t('edit_model', model: Practice.model_name.human)

= render 'form'
